[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tensor-public"
version = "0.1.0"
description = "Public mathematical specification and reproducibility implementation for the Tensor theoretical preprint"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
